# React + Vite (cleaned)

Minimal React + Vite starter. Files and example assets removed to give you a clean starting point.

Run locally:

```
npm install
npm run dev
```

Files modified:
- `src/App.jsx` simplified
- `src/index.css` reduced to minimal styles
- example assets removed from `src/assets` and `public`
